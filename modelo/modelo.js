/**
 * Clase Modelo que gestiona un mapa de datos.
 */
export class Modelo {
  /**
   * Crea una instancia de Modelo.
   */
  constructor () {
    /**
     * Mapa de datos gestionado por el modelo.
     * @type {Map}
     */
    this.mapa = new Map()
  }
}
